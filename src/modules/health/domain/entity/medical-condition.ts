export class MedicalCondition {
  private id: string;
  private name: string; // Name of the medical condition (e.g., arthritis, diabetes).
  private symptoms: string;
  private isChronic: boolean;
}
